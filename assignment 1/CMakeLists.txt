# Defines the executable for this specific experiment
add_executable(exp01_lexer main.cpp)

# Later on, you will link LLVM or other dependencies here like:
# target_link_libraries(exp01_lexer PRIVATE LLVMSupport LLVMCore ...)